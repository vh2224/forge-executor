/** Extension-stable re-export — implementation lives in @gsd/agent-core. */
export type { Keybinding, KeybindingsConfig } from "@gsd/pi-tui";
export {
	type AppAction,
	type AppKeybinding,
	KeybindingsManager,
} from "@gsd/agent-core/keybindings.js";
